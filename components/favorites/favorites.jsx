import React from "react";
import { StyleSheet, View, Text } from "react-native";

const Favorites = () => {
  return (
    <View>
      <Text style={{ color: "wheat" }}>Fav</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Favorites;
